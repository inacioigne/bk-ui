import { Container, Box, Skeleton, Grid } from "@mui/material/";
export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return (<Container maxWidth="xl">
      <h1>Espere Autority...</h1>

    </Container>
    )
  }